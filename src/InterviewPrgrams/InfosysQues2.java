package InterviewPrgrams;

import java.time.Duration;
import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class InfosysQues2 {
	/*public static void main(String[] args) {
		//Programs to identify all the links of languages in google page.
		//Infosys interview questions.
		//WebDriver driver = new ChromeDriver();
		
		 * driver.manage().window().maximize();
		 * driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(2000));
		 * List<WebElement> alllinks =
		 * driver.findElements(By.xpath("//div[@id='SIvCob']/a"));
		 * 
		 * for ( WebElement link : alllinks) { String link_text = link.getText();
		 * System.out.println(link_text); }
		 * 
		 * 
		 * 
		 
		
	}
	
*/
}
